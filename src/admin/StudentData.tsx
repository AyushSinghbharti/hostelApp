import { Button, StyleSheet, Text, TouchableOpacity, View } from "react-native";
import React from "react";

const StudentData = () => {
  return (
    <View><Text>Student data</Text></View>
  );
};

export default StudentData;

const styles = StyleSheet.create({});
